apiVersion: v1
kind: Service
metadata:
  name: streaming
spec:
  selector:
    deployment: streaming
  type: NodePort
  ports:
  - protocol: TCP
    port: 4000
    targetPort: 4000
  sessionAffinity: ClientIP
